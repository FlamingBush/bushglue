[project]
name = "bushutil"
version = "0.1.0"
description = "Shared utilities for Bush Glue services"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
