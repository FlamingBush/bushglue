#!/bin/sh
# Called by udev when the TI PCM2912A USB codec appears.
# NOTE: amixer sset on this device triggers usb_set_interface failures on the
# RK3568 OHCI controller even for simple mute/volume operations. All mixer
# initialisation is handled by the bush-monitor _codec_watch_thread instead,
# which runs 3s after plug-in when the device is fully settled.
exit 0
