[project]
name = "t2v-common"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "chromadb>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
